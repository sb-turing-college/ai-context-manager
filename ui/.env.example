# ============================================================
# AI Context Manager – UI environment (OPTIONAL)
# Not required for local start-all. Defaults are baked into the UI.
# Copy to ui/.env only if you need a non-default API URL (e.g. Docker).
# ============================================================

# Backend base URL used by the Vite app (default: http://127.0.0.1:8000)
VITE_API_URL=http://127.0.0.1:8000
